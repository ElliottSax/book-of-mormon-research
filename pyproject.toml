[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[project]
name = "bom-research"
version = "0.1.0"
description = "Book of Mormon authorship-research analytics toolkit"
requires-python = ">=3.10"
dependencies = ["jellyfish>=1.0"]

[project.optional-dependencies]
dev = ["pytest"]
